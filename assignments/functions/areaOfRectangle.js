function areaOfRectangle(length,breadth){
    let area= length*breadth;
    //we are using variable area to hold value of area of rectangle so we can use it whenever it is needed
    return area;
}
let area=areaOfRectangle(11,6);
//now the fcn executes multiplication l*b as the 11,6 arguments are defined.
console.log(area);
//now the value of areaOfRectangle which is stored in variable area is printed.